Recibiste creditos

Hola, <%= @customer.name %>.

<%= @business_name %> te ha dado <%= number_to_currency(@amount, unit: "$", precision: 0, delimiter: ".") %> en creditos.

<%= "Motivo: #{@description}" if @description.present? %>

Tu saldo actual es de <%= number_to_currency(@new_balance, unit: "$", precision: 0, delimiter: ".") %>. Puedes usarlos en tu proxima reserva.

Reservar: <%= @booking_url %>
