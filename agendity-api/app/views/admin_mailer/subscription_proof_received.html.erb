<h2>Nuevo comprobante de pago recibido</h2>

<p>El negocio <strong><%= @business.name %></strong> ha enviado un comprobante de pago para activar su suscripcion.</p>

<table class="detail-table">
  <tr>
    <td>Negocio</td>
    <td><%= @business.name %></td>
  </tr>
  <tr>
    <td>Propietario</td>
    <td><%= @business.owner.name %> (<%= @business.owner.email %>)</td>
  </tr>
  <% if @plan %>
    <tr>
      <td>Plan</td>
      <td><%= @plan.name %></td>
    </tr>
  <% end %>
  <tr>
    <td>Monto</td>
    <td>$<%= @order.amount.to_f.round(0) %> COP</td>
  </tr>
  <tr>
    <td>Fecha</td>
    <td><%= @order.proof_submitted_at&.strftime('%d/%m/%Y %H:%M') %></td>
  </tr>
</table>

<% if @admin_url.present? %>
  <div style="text-align: center; margin: 20px 0;"><a href="<%= @admin_url %>/admin/subscription_payment_orders/<%= @order.id %>" style="display: inline-block; background-color: #7c3aed; color: #ffffff; padding: 12px 32px; border-radius: 6px; font-size: 16px; font-weight: 600; text-decoration: none;">Revisar en el panel</a></div>
<% end %>

<p>— Agendity System</p>
